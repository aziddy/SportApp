<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.alexander.sportapp">

    <uses-permission android:name="com.vogella.android.locationapi.maps.permission.MAPS_RECEIVE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:launchMode="singleTask"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name"
            android:launchMode="singleTask"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".FindHost"
            android:label="@string/title_activity_find_host"
            android:launchMode="singleTask"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".clientProfile"
            android:label="@string/title_activity_client_profile"
            android:launchMode="singleTask"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".FindEventActivity"
            android:label="@string/title_activity_find"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".FindEventDetailActivity"
            android:label="@string/title_activity_find_event_detail"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <!--
             ATTENTION: This was auto-generated to add Google Play services to your project for
             App Indexing.  See https://g.co/AppIndexing/AndroidStudio for more information.
        -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name=".SignUpActivity"
            android:label="@string/title_activity_sign_up"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Host_PickSport"
            android:label="@string/title_activity_host__pick_sport"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Host_PickDateTime"
            android:label="@string/title_activity_host__pick_time"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Host_PickLocation"
            android:label="@string/title_activity_host__pick_location"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Host_PickAge"
            android:label="@string/title_activity_host__pick_age"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Host_ReviewEvent"
            android:label="@string/title_activity_host__review_event"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".HostLeague_CreateOne"
            android:label="@string/title_activity_host_league__create_one"
            android:theme="@style/AppTheme.NoActionBar"
            android:windowSoftInputMode="stateHidden" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".ManageMyPickups"
            android:label="@string/title_activity_manage_my_events"
            android:launchMode="singleTask"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ManageMyLeagues"
            android:launchMode="singleTask"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".HostLeagueOrPickup"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ManageMyLeaguesMatches"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ManageMyLeaguesTeams"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".CreateNewTeamLeague"
            android:theme="@style/AppTheme.NoActionBar"
            android:windowSoftInputMode="stateHidden|adjustNothing" />
        <activity
            android:name=".UsernameFind"
            android:theme="@style/Theme.AppCompat.Dialog"
            android:windowSoftInputMode="stateHidden" />
        <activity
            android:name=".ColorPicker"
            android:theme="@style/Theme.AppCompat.Dialog" />


        <activity android:name=".TeamLeagueDetail"
            android:theme="@style/AppTheme.NoActionBar" />
            />
    </application>

</manifest>
